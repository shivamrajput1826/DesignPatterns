public class Learning {
    // I have created a notification concrete class, which has all the notification params
    // I have created a notification client => interface, this interface has a notify method
    // I have created a client => client implements  notificationclient
    // 2 product clients ->Mail Notification Client, SMS notification Client
    // extends the  client
    // Notification Factory => it basically get a client enum and based on that it provides
    // which notification client to go;
    // now in the main function, i am first creating a notification, create a client based on
    // the enum, and call notify method{

 }
